_base_: scn_convnext.yaml
model:
  backbone_mode: native
  preserve_pretrained_norm: true
  in_channels: 3
  use_spatial_attention: false
  use_latent_graph: false
scn:
  use_scn: false
data:
  input_size: 224
  normalization: imagenet
# Standalone native ConvNeXt baseline; retraining required. Not a drop-in old checkpoint config.
