# Legacy Files

This folder keeps old experiment surfaces that are not part of the current
Pixel Motif end-to-end workflow.

Current workflow:

```text
kaggle_pixel_motif_end_to_end.ipynb
scripts/run_experiment.py
configs/experiments/pixel_motif_baseline_b.yaml
configs/experiments/hierarchical_motif_gnn_c.yaml
```

Kept active configs:

```text
configs/pixel_motif_guided_gnn_motif_norm.yaml
configs/hierarchical_motif_gnn.yaml
```

The files here are not deleted so older ideas can still be inspected, but they
should not be used for new Kaggle runs unless they are deliberately restored.

