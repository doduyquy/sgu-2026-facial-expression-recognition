_base_: "rafdb_mask_guided_c_full_mgr_kaggle.yaml"

# Full MGR RAF-DB alpha sweep: slightly weaker mask prior than alpha 0.3.

model:
  mask_attention_alpha: 0.25

paths:
  output_dir: "/kaggle/working/outputs/rafdb_mgr_full_a025"
