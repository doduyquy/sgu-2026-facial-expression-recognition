data:
  mode: motif_filtered
  motif_filtered_dataset_path: artifacts/motif_filtered_dataset_v1
  batch_size: 128
  num_workers: 2
  pin_memory: true

model:
  name: motif_guided_mlp
  input_dim: null
  hidden_dim: 128
  num_classes: 7
  dropout: 0.3
  use_motif_score_vector: true
  use_match_score_weighting: true

loss:
  name: weighted_ce_motif
  use_class_weights: true
  class_counts: [3995, 436, 4097, 7215, 4830, 3171, 4965]
  lambda_motif: 0.1
  margin: 0.2

optimizer:
  name: AdamW
  lr: 0.001
  weight_decay: 0.0001

scheduler:
  name: ReduceLROnPlateau
  mode: max
  monitor: val_macro_f1
  factor: 0.5
  patience: 4
  min_lr: 0.000001

training:
  epochs: 60
  early_stopping_patience: 12
  monitor: val_macro_f1
  grad_clip_norm: 5.0

logging:
  use_wandb: true
