# Config mặc định CHUNG cho toàn bộ project GNN FER-2013
# Các file config yaml khác sẽ inherit thằng này và ghi đè lại
# ----------- ----------- -----------

data:
  name: "fer13-split"
  num_classes: 7    # angry, disgust, fear, happy, neutral, sad, surprise
  image_size: 48
  batch_size: 128
  num_workers: 2    # Kaggle: 2 | Windows local: 0

seed:
  random_seed: 42

graph:
  connectivity: 8         # 4 hoặc 8
  normalize_pixels: true  # normalize về [0, 1]
  node_features:
    - "intensity"
    - "x_norm"
    - "y_norm"
  # Mở rộng sau: "gx", "gy", "grad_mag", "contrast"
  edge_features:
    - "dx"
    - "dy"
    - "dist"
    - "delta_intensity"
    - "intensity_similarity"
  intensity_similarity_alpha: 1.0
  save_image_in_graph: false

model:
  name: "mlp_baseline"   # key trong MODEL_REGISTRY
  hidden_dims: [64, 32]
  dropout: 0.2

training:
  epochs: 30
  patience: 10
  weight_decay: 0.0001
  loss: "cross_entropy"
  optimizer: "adam"      # adam | sgd
  lr: 0.001
  scheduler: "none"      # none | step | cosine | reduce_lr_on_plateau

# !!! WARNING !!!
logging:
  use_wandb: false
  wandb_entity: "phucga15062005"
  wandb_project: "GNN-FER2013"

# !!! WARNING !!!
env:
  platform: "kaggle"    # local | kaggle  <--- MẶC ĐỊNH KAGGLE

# !!! WARNING !!!
kaggle:
  data_path: "/kaggle/input/datasets/doduyquynii/fer13-split"
  root_path: "/kaggle/working/sgu-2026-facial-expression-recognition"
  # Thay YOUR_GRAPH_CACHE_DATASET bằng tên dataset .pt trên Kaggle của bạn
  # VD: "/kaggle/input/fer2013-graph-cache"
  graph_cache_path: "/kaggle/input/YOUR_GRAPH_CACHE_DATASET"

local:
  data_path: "data/fer13-split"
  root_path: "."
  graph_cache_path: "outputs/graph_cache"