_base_: "reproduce_7512_mask_guided_7462_learned_only_kaggle.yaml"

# Seed sweep run for the 75.12 single-checkpoint recipe.
#
# Keep the 75.12 training scenario unchanged:
# - FER-trained ConvNeXt 74.62 backbone initialization
# - mask-guided region attention
# - learned-only region tokens
# - no CLIP dictionary / no learned+CLIP mixing
#
# Only change the random seed and disable WandB. All artifacts are written to
# Kaggle working output so the run can be saved as a Kaggle output version.

seed:
  random_seed: 3407

paths:
  output_dir: "/kaggle/working/outputs/seed_sweep_7512/seed_3407"

logging:
  use_wandb: false

