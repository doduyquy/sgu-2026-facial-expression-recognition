# khuyến khích chạy trên kaggle cho đồng bộ
local:
  data_path: "dataset/fer13-split" # Points to the folder containing train|val|test.csv
  output_dir: 'outputs'
  num_workers: 0

kaggle:
  # Link dataset: /kaggle/input/datasets/doduyquynii/fer13-split
  data_path: "/kaggle/input/datasets/doduyquynii/"  
  output_dir: "/kaggle/working/outputs"
  num_workers: 2