_base_: "c_cnn_region_logits_080_020_imagenet_sam_cosine_seed42_noclean_batch32_kaggle.yaml"

# Backbone generalization ablation.
# Method stays C:
#   final = 0.8 * cnn_aux_logits + 0.2 * region_attention_logits
# Only the visual backbone changes: ConvNeXt-Tiny -> ResNet18.

model:
  arch: "resnet18"
  pretrained: true
  weights: "DEFAULT"
  checkpoint_path: null
  checkpoint_strict: false

training:
  epochs: 150
  patience: 30

paths:
  output_dir: "/kaggle/working/outputs/paper_ablation_strong_c_resnet18_imagenet_seed42_noclean_batch32"
